# Changelog

## v0.2.1

- Added MIT license text.
- Added GitHub Actions test workflow.
- Added concise English README and Japanese README.
- Added demo documentation and Codex for Open Source application draft.

## v0.2.0

- Added `ctx handoff "message"` as the beginner-friendly daily workflow.
- Added a friendly guide when running `ctx` with no subcommand.
- Added Japanese output with `--lang ja`.
- Added `ctx config` for project defaults.
- Added `ctx doctor` for environment checks.
- Hid `.ctx-ledger/` generated files from changed-file snapshots.

## v0.1.0

- Initial Python CLI package.
- Added `ctx init`, `ctx note`, `ctx snap`, `ctx ask`, `ctx sent`, and `ctx status`.
- Added Markdown context pack generation.
- Added secret redaction and basic tests.
